import { AppDateSuffixPipe } from './app-date-suffix.pipe';

describe('AppDateSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new AppDateSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});

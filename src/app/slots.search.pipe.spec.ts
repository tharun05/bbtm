import { Slots.SearchPipe } from './slots.search.pipe';

describe('Slots.SearchPipe', () => {
  it('create an instance', () => {
    const pipe = new Slots.SearchPipe();
    expect(pipe).toBeTruthy();
  });
});
